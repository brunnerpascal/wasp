{
  "name": "faucet",
  "version": "1.0.0",
  "scripts": {
    "asbuild:untouched": "asc faucet.ts --target debug",
    "asbuild:optimized": "asc faucet.ts --target release",
    "asbuild": "npm run asbuild:untouched && npm run asbuild:optimized",
    "test": "node tests"
  },
  "dependencies": {
    "@assemblyscript/loader": "^0.19.18"
  },
  "devDependencies": {
    "assemblyscript": "^0.19.18"
  }
}